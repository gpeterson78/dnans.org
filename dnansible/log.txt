sudo apt update
    2  sudo apt dist-upgrade
    3  sudo apt install -y ansible
    sudo apt update && sudo apt install -y git && git config --global user.name "grady@gradyp.com" && mkdir -p ~/git/dnansible && cd ~/git/dnansible && git clone <your_repo_url> .

